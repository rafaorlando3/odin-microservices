server:
  port: 8888
spring:
  application:
    name: odin-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rafaorlando3/odin-config-server
          default-label: main
          search-paths:
          - 'greeting-service*'